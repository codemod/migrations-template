schema_version: "1.0"

# Replace @scope with your org name; name must start with your scope for registry visibility
name: "@scope/jssg-codemod"
version: "0.1.0"
description: "Transform legacy code patterns"
author: "Author <author@example.com>"
license: "MIT"
workflow: "workflow.yaml"
category: "migration"

targets:
  languages: ["typescript"]

keywords: ["transformation", "migration"]

registry:
  access: "public"
  visibility: "public"
